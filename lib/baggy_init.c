char* baggy_size_table;

void baggy_init() {
	static bool is_initialized = false;
	if (!is_initialized) {
		is_initialized = true;

		// TODO setup the baggy_size_table and the page fault handler
	}
}
